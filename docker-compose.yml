# Docker Compose 配置
# 当前无服务配置（Redis 已移除，使用本地 PostgreSQL）

version: '3.8'

services:
  # PostgreSQL 数据库（可选 - 如果使用本地数据库则不需要）
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: cost-postgres
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     POSTGRES_USER: cost
  #     POSTGRES_PASSWORD: cost_password
  #     POSTGRES_DB: cost_management
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   restart: unless-stopped

# volumes:
#   postgres-data:
